import React from "react"
import Section from "src/components/Section"

const Calculation: React.FC<{}> = (props) => {
  const { ...foo } = props

  return <Section>Calculation</Section>
}

export default Calculation
